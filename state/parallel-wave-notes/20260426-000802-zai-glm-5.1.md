# Parallel Wave Note: 20260426-000802 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `4fabce67e6b81527623af68e95a22ee61f757bdb..HEAD` before new work. This isolated wave worktree was exactly at `4fabce6`, so there were zero commits in range and no range-review patch was required.

Also inspected latest parallel notes, `state/autopilot.md`, and the recorded other-model heads (`anthropic/claude-opus-4.7@c5644ce`, `openai-codex/gpt-5.5@5f9cfae`). Recent SOD static/pickup, presentation RGBA, and audio offset validation work is headless-test-backed and avoids `source/original/` and proprietary game-file assets. No corrective issue was found before this cycle's small audio-boundary hardening.

## Work Done

- Tightened `wl_validate_audio_header_offsets()` so the first AUDIOHED data offset is also required to be within the paired AUDIOT file size, not just later offsets and the sentinel.
- Added WL6 regression coverage for a corrupt first offset past EOF with a matching monotonic second offset, verifying both direct validation and guarded `wl_read_audio_chunk()` reject it before reading.
- Updated `docs/research/audio-assets.md` to document the all-offset bound contract.

## Verification

```bash
cd source/modern-c-sdl3
make test && make sdl3-info test-sdl3
```

Result: headless asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for `../../../game-files/base`; SDL3 smoke skipped gracefully with `HAVE_SDL3=no` because SDL3 development files are unavailable in this worktree.

## Blockers

None for this headless audio offset validation seam. SDL3-backed smoke remains gated on SDL3 development files.
