# Parallel Wave Note: 20260426-005956 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `ef066855f1f911da486f5b73c6c0091eb2461d81..HEAD`; this isolated worktree had zero commits in that range, so no prior-model corrective patch was required.

Also inspected `docs/AUTOPILOT.md`, `state/autopilot.md`, the latest parallel-wave notes, and the recorded other-model heads (`anthropic/claude-opus-4.7@c5644ce`, `openai-codex/gpt-5.5@cd5933a`). Recent presentation, actor, SOD, and audio metadata work is headless-test-backed, avoids `source/original/`, and does not commit game-file assets. No concrete review issue was found before continuing.

## Work Done

- Tightened the pitched RGBA present-frame expansion contract so `wl_expand_present_frame_to_rgba_pitched()` now requires the full `pitch * height` accessible span, matching upload descriptor and padding-clear preflights.
- Updated regression coverage for undersized padded RGBA buffers even when no output upload descriptor is requested.
- Updated SDL3 presentation research notes to record the consistent full-span pitched-RGBA contract.

## Verification

```bash
cd source/modern-c-sdl3
make test && make sdl3-info test-sdl3
```

Result: headless asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for `../../../game-files/base`; SDL3 smoke skipped gracefully with `HAVE_SDL3=no` because SDL3 development files are unavailable in this worktree.

## Blockers

None for this headless presentation contract hardening. SDL3-backed smoke remains gated on SDL3 development files.
