# Parallel Wave Note: 20260425-191457 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `f58300a4eab765179f209f8bfbb9741efb7a3a34..HEAD`. Prior other-model work present was `anthropic/claude-opus-4.7@58c318f` (merged at `ed400b9`), which broadened SOD IMF playback coverage in `tests/test_assets.c` and `docs/research/audio-assets.md`.

Review finding: the prior commit looked sound: metadata-only assertions, no proprietary asset commits, and docs matched the new SOD playback seam. While reviewing it, I noticed the existing `wl_describe_audio_chunk()` API is WL6-range-specific, so using it directly for SOD music chunks would misclassify SOD chunk 243 as digital. I fixed that as a small follow-up in this cycle rather than starting unrelated feature work.

## Work Done

- Added `wl_describe_audio_chunk_with_ranges()` so callers can describe AUDIOHED/AUDIOT chunks with explicit PC speaker / AdLib / digital range counts.
- Kept `wl_describe_audio_chunk()` as the WL6-compatible wrapper using the original 87/87/87 ranges.
- Added headless assertions for WL6 wrapper parity and overflow rejection.
- Added optional SOD assertions proving chunk 243 is classified as music with SOD's 81/81/81 range split while the legacy WL6 wrapper would classify that same index as digital.
- Updated `docs/research/audio-assets.md` with the generic range descriptor seam.

## Verification

```bash
cd source/modern-c-sdl3
make test
```

Result: passed. `asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for ../../../game-files/base`.

## Blockers

None expected. SDL3 development files may still be unavailable for SDL-backed smoke tests, but this cycle's change is SDL-free.
