# Parallel Wave Note: 20260425-191134 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `01e2b264c6a3cb4deaaf63c21d46c172c7a518ca..HEAD` in this worktree. Prior other-model work present in range was `anthropic/claude-opus-4.7@8230f37` (merged at `f9ddc16`), which pinned remaining WL6 audio range summaries in `tests/test_assets.c` and `docs/research/audio-assets.md`.

Review finding: no correctness, safety, asset, or verification issue found. The commit only added metadata/count/hash assertions and a wave note; no proprietary game-file assets were committed.

## Work Done

- Added `wl_adlib_instrument_registers` and `wl_describe_adlib_instrument_registers()`.
- The helper maps the existing 16-byte AdLib instrument block into deterministic channel-0 OPL modulator/carrier register/value pairs for `0x20/0x23`, `0x40/0x43`, `0x60/0x63`, `0x80/0x83`, and `0xe0/0xe3`, plus feedback register/value, voice, and mode metadata.
- Added WL6 chunk `87` headless assertions for register numbers, representative values, feedback/voice/mode, and invalid arguments.
- Updated `docs/research/audio-assets.md` with the new AdLib register descriptor seam.

## Verification

```bash
cd source/modern-c-sdl3
make test
```

Result: passed.

## Blockers

- None for this headless AdLib characterization seam. SDL3 presentation/audio output remains limited by unavailable SDL3 development files in this environment.
