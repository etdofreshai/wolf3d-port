# Parallel Wave Note: 20260426-001023 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `0b80a91c857069cb970ea66052ec59035f2868c5..HEAD`; this isolated worktree was exactly at `0b80a91`, so there were zero commits in range and no corrective prior-model review patch was required.

Also read `docs/AUTOPILOT.md`, `state/autopilot.md`, and recent parallel notes. The latest audio-boundary, presentation-layout, and SOD pickup/classification work is headless-test-backed, does not touch `source/original/`, and does not commit proprietary game-file assets. No concrete issue was found before this cycle's small presentation hardening.

## Work Done

- Hardened `wl_expand_indexed_surface_to_rgba()` so it rejects indexed surfaces whose RGBA row pitch cannot be represented in the renderer upload descriptor's `uint16_t pitch` field.
- Added a headless synthetic regression with a 16,384-pixel-wide indexed surface, verifying expansion fails instead of returning a truncated RGBA pitch.

## Verification

```bash
cd source/modern-c-sdl3
make test
make sdl3-info test-sdl3
```

Result: headless asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed. SDL3 smoke skipped gracefully with `HAVE_SDL3=no` because SDL3 development files are unavailable in this worktree.

## Blockers

None for this headless presentation descriptor hardening. SDL3-backed smoke remains gated on SDL3 development files.
