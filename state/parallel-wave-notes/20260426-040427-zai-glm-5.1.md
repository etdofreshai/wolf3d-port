# Parallel Wave Note: 20260426-040427 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `1f883233b3526460def52adc933e43426e528bc4..HEAD` in this isolated worktree. The range contained zero commits here, so no range-specific prior-model corrective review was required.

Also inspected `docs/AUTOPILOT.md`, `state/autopilot.md`, and the latest visible parallel-wave note. The current branch is at `1f88323` before this cycle, with prior work headless-test-backed and no unsafe edits to `source/original/` or game-file assets. No concrete corrective issue was found before continuing.

## Work Done

- Extended `wl_player_use_result` with keyed-door metadata: `required_key` and `has_required_key`.
- `wl_use_player_facing()` now records the zero-based required key bit for lock ids 1-4 and whether the player currently has it before returning locked/opened status.
- Updated the existing keyed-door use-button test to pin missing-key and has-key metadata.
- Documented the headless use-button metadata seam in the modern C README and runtime model research note.

## Verification

```bash
cd source/modern-c-sdl3
make test
make sdl3-info test-sdl3
```

Result: asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for `../../../game-files/base`; SDL3 smoke skipped gracefully with `HAVE_SDL3=no` because SDL3 development files are unavailable in this worktree.

## Blockers

None. SDL3-backed smoke remains gated on SDL3 development files when unavailable.
