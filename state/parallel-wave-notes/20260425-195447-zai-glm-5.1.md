# Parallel Wave Note: 20260425-195447 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `c31a82f169589dd5358e74d65722c3d8236c0aa1..HEAD` before new work. Concrete prior model work in range was `anthropic/claude-opus-4.7@899348e` (`Add metadata-backed sound schedule tick seam`), merged at `ce5e5f4`.

- The prior helper validates non-empty PC speaker/AdLib metadata, rejects oversized chunk ids and malformed payloads, and only ticks newly accepted/replaced candidates so lower-priority held candidates do not advance unrelated active audio.
- Tests and docs match the implementation, and no `source/original/` or proprietary game-file assets were changed.
- No corrective issue was found.

## Work Done

- Added `wl_schedule_advance_sound_channel_from_chunk()` as a metadata-backed scheduling + state-only advancement seam for future audio scheduling/mixer code that needs updated channel state/completion without decoding a current sample byte.
- The helper mirrors schedule+tick validation, starts/replaces accepted PC speaker/AdLib candidates, preserves held lower-priority active channels without advancing, and rejects oversized chunk ids, truncated payloads, and null output.
- Added WL6 headless assertions for inactive PC-speaker schedule+advance, lower-priority hold/no-advance, oversized chunk id rejection, truncated payload rejection, and null-output rejection.
- Updated `docs/research/audio-assets.md` with the new seam and coverage note.

## Verification

```bash
cd source/modern-c-sdl3
make test
make sdl3-info test-sdl3
```

Result: headless asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for `../../../game-files/base`; SDL3 smoke gracefully skipped with `HAVE_SDL3=no` because SDL3 development files are unavailable in this worktree.

## Blockers

None for this SDL-free audio scheduling seam. SDL3-backed smoke remains limited to graceful skip behavior until SDL3 development files are available.
