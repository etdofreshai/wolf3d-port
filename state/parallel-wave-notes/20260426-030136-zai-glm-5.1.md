# Parallel Wave Note: 20260426-030136 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `a7971e8f627ac6b78f33ae1e41cff518274dab47..HEAD` in this isolated worktree. The range contained zero commits, so no range-specific prior-model corrective review was required.

Also inspected `docs/AUTOPILOT.md`, `state/autopilot.md`, and recent parallel-wave notes including the recorded other-model heads `anthropic/claude-opus-4.7@c5644ce` and `openai-codex/gpt-5.5@dd5fd40`. The visible prior actor-state, SOD/static, presentation, and audio work is headless-test-backed, avoids `source/original/`, and does not commit proprietary game-file assets. No concrete corrective issue was found before continuing.

## Work Done

- Extended `wl_actor_threat_summary` with `invalid_mode_count` so shootable actors carrying unknown runtime modes are visible to diagnostics instead of being only folded into the inert bucket.
- Kept backward-compatible inert accounting for unknown modes while adding synthetic headless coverage for the invalid-mode bucket.
- Updated runtime model research notes and the modern C README.

## Verification

```bash
cd source/modern-c-sdl3
make test
make sdl3-info test-sdl3
```

Result: headless asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for `../../../game-files/base`; SDL3 smoke skipped gracefully with `HAVE_SDL3=no` because SDL3 development files are unavailable in this worktree.

## Blockers

None. SDL3-backed smoke remains gated on SDL3 development files when unavailable.
