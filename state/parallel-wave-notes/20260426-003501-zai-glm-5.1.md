# Parallel Wave Note: 20260426-003501 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `9a5fafc07c2d83aff36a028bd532240ee43e1255..HEAD`; this worktree started at `9a5fafc`, so there were zero commits in range. I also inspected latest notes and `state/autopilot.md`; recent WL6 classification and presentation helper work is test-backed and did not require a corrective patch.

## Work Done

- Added optional SOD cached sprite-surface coverage for real `Tunnels 1` Spear-aware scene refs.
- The test decodes the first and last collected SOD scene refs through `wl_decode_vswap_sprite_surface_cache()` and pins the combined cache hash plus per-surface hashes.
- Updated the SOD runtime gap research note with the new renderer-facing coverage.

## Verification

```bash
cd source/modern-c-sdl3
make test
```

Result: asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for `../../../game-files/base`.

## Blockers

None. SDL3-backed presentation smoke remains gated on SDL3 development files when unavailable.
