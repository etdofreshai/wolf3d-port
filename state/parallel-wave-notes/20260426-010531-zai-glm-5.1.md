# Parallel Wave Note: 20260426-010531 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `f18f80511559a8d31ede3834413690f287e658ee..HEAD`; this isolated worktree had zero commits in that range, so no prior-model corrective patch was required.

Also inspected `docs/AUTOPILOT.md`, `state/autopilot.md`, and latest parallel-wave notes. Recent presentation, SOD, audio, actor-state, and model-summary work is documented as headless-test-backed, avoids `source/original/`, and does not commit game-file assets. No concrete review issue was found before continuing.

## Work Done

- Added `wl_count_actors_by_mode()` as a deterministic runtime model summary helper parallel to `wl_count_actors_by_kind()`.
- Routed the broader WL6 map/boss/secret model coverage through mode histogram assertions computed from actor descriptors.
- Added undersized-capacity rejection coverage for the new helper.
- Updated modern C README and runtime model research notes.

## Verification

```bash
cd source/modern-c-sdl3
make test
make sdl3-info test-sdl3
```

Result: headless asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for `../../../game-files/base`; SDL3 smoke skipped gracefully with `HAVE_SDL3=no` because SDL3 development files are unavailable in this worktree.

## Blockers

None for this model-summary seam. SDL3-backed smoke remains gated on SDL3 development files.
