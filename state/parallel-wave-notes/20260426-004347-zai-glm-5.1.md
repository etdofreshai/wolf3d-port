# Parallel Wave Note: 20260426-004347 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `076b697713b1e5a0b8ac19fbd185598f92ea964f..HEAD`; this isolated wave worktree had zero commits in that range, so no range-specific prior-model corrective patch was required.

Also inspected `docs/AUTOPILOT.md`, `state/autopilot.md`, latest parallel-wave notes, and the recorded other-model heads (`anthropic/claude-opus-4.7@c5644ce`, `openai-codex/gpt-5.5@cd5933a`). Recent actor wake/chase, SOD scene-ref/cache, WL6 classification, presentation helper, and audio-boundary work is headless-test-backed and does not touch `source/original/` or proprietary game files. No concrete review issue was found before continuing.

## Work Done

- Extended `wl_actor_wake_all_result` with `first_woken_actor` and `last_woken_actor` diagnostics.
- Initialized the indexes to `UINT16_MAX` when a batch wake considers actors but wakes none.
- Updated `wl_wake_actors_for_chase()` to record the first and last actor indexes that transition into chase mode.
- Added headless assertions for non-ambush wake, later ambush-inclusive wake, and a no-new-wake batch preserving sentinel indexes.
- Updated runtime model research notes with the aggregate wake diagnostics.

## Verification

```bash
cd source/modern-c-sdl3
make test && make sdl3-info test-sdl3
```

Result: asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for `../../../game-files/base`; SDL3 smoke skipped gracefully with `HAVE_SDL3=no` because SDL3 development files are unavailable in this worktree.

## Blockers

None. SDL3-backed smoke remains gated on SDL3 development files when unavailable.
