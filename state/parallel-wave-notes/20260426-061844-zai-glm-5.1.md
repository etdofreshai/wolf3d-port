# Parallel Wave Note: 20260426-061844 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `9ef2baccaba47986b7a2d8170e7d5db41dd5147d..HEAD` at cycle start. This isolated worktree was exactly at `9ef2bac`, so the range contained zero commits and no range-specific prior-model corrective review was required.

Also inspected `docs/AUTOPILOT.md`, `state/autopilot.md`, and recent parallel-wave notes including recorded other-model heads `anthropic/claude-opus-4.7@c5644ce` and `openai-codex/gpt-5.5@dd5fd40`. Visible recent interaction/tile diagnostics are headless-test-backed, avoid `source/original/`, and do not commit proprietary game-file assets. No concrete corrective issue was found before continuing.

## Work Done

- Extended `wl_runtime_tile_diagonal_summary` with marker sub-buckets for door markers, moving-pushwall markers, and other runtime markers on both live tilemap diagonals.
- Updated `wl_summarize_runtime_tile_diagonals()` to preserve the existing aggregate marker counts while exposing those blocker-class counts.
- Broadened synthetic headless diagonal coverage to pin wall, door, pushwall, other-marker, and clear-floor buckets on both diagonals.
- Updated runtime model docs and the modern C README.

## Verification

```bash
cd source/modern-c-sdl3
make test-headless && make test
```

Result: pure-C headless harness passed for `../../../game-files/base`. `make test` also passed the headless harness and skipped optional SDL3 smoke tests because SDL3 development files are unavailable in this worktree.

## Blockers

None currently. SDL3-backed smoke remains gated on SDL3 development files when unavailable.
