# Parallel Wave Note: 20260425-200553 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `4e24ff1bad6eb09b5df845b27aff703d230ecfe2..HEAD`. The concrete prior model work in range was `anthropic/claude-opus-4.7@062c3e6`, especially worker commit `bed931c` (`Add metadata-backed sound schedule window seam`).

- The helper validates candidate PC speaker/AdLib metadata and bounded payload size before exposing a reset playback window.
- Held lower-priority candidates leave the active channel intact and do not inspect the candidate sample bytes, matching the scheduler behavior.
- Tests and docs cover accept/hold/invalid paths; no `source/original/` edits or proprietary game data were present. No corrective issue was found.

## Work Done

- Added `wl_schedule_describe_sound_channel_position_from_chunk()` as the position-oriented sibling to the schedule+window seam.
- The helper schedules metadata-backed PC speaker/AdLib candidates, reports reset playback position/sample count for accepted sounds, and preserves held lower-priority active channels without sample inspection.
- Added WL6 headless assertions for inactive PC-speaker schedule+position, lower-priority hold/no-position, oversized chunk id rejection, truncated payload rejection, and null-output rejection.
- Updated `docs/research/audio-assets.md` with the new schedule+position seam.

## Verification

```bash
cd source/modern-c-sdl3
make test && make sdl3-info test-sdl3
```

Result: headless asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for `../../../game-files/base`; SDL3 smoke gracefully skipped with `HAVE_SDL3=no` because SDL3 development files are unavailable in this worktree.

## Blockers

None for this SDL-free audio scheduling/position seam.
