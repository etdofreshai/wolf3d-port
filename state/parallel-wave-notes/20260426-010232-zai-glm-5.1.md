# Parallel Wave Note: 20260426-010232 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `0128d1714cd8edc453db6bfe9c8b79485b7a677a..HEAD`; this isolated worktree had zero commits in that range, so no prior-model corrective patch was required.

Also inspected `docs/AUTOPILOT.md`, `state/autopilot.md`, and the latest parallel-wave notes. Recent presentation/audio/SOD/runtime work is documented as headless-test-backed, avoids `source/original/`, and does not commit game-file assets. No concrete review issue was found before continuing.

## Work Done

- Added `wl_count_actors_by_kind()` as a small runtime model helper for deterministic actor-kind summaries.
- Routed the existing broader WL6 boss/secret map actor-kind assertions through the helper.
- Added negative coverage for undersized output capacity.

## Verification

```bash
cd source/modern-c-sdl3
make test && make sdl3-info test-sdl3
```

Result: headless asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for `../../../game-files/base`; SDL3 smoke skipped gracefully with `HAVE_SDL3=no` because SDL3 development files are unavailable in this worktree.

## Blockers

None for this model-summary helper. SDL3-backed smoke remains gated on SDL3 development files.
