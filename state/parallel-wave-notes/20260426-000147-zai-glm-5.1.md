# Parallel Wave Note: 20260426-000147 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `785e0a5c1fa8fb2fd377d064dbe2df335bfeb37b..HEAD` before new work. This isolated wave worktree was exactly at `785e0a5`, so there were zero commits in range and no range-review patch was required.

Also inspected the recorded other-model heads:

- `anthropic/claude-opus-4.7@c5644ce` classified optional SOD static/info-plane gaps and adjusted SOD runtime model coverage.
- `openai-codex/gpt-5.5@5f9cfae` tightened present-frame RGBA expansion/layout validation and added undersized/mismatched-frame regressions.

No source/original or proprietary game-file asset changes were present in the reviewed summaries; recent work is headless and test-backed. No corrective issue was found before this cycle's small audio-boundary hardening.

## Work Done

- Added `wl_validate_audio_header_offsets()` to validate AUDIOHED offset monotonicity and ensure offsets/sentinel stay within the paired AUDIOT size.
- Routed `wl_read_audio_chunk()` through that validator before trusting offsets, so a corrupted sentinel or offset past EOF is rejected before file seeking/reading.
- Added WL6 regression coverage for valid offset tables plus a one-byte-past-EOF sentinel that must fail validation and chunk reading.
- Added optional SOD validation coverage and updated `docs/research/audio-assets.md` with the offset-table contract.

## Verification

```bash
cd source/modern-c-sdl3
make test
make sdl3-info test-sdl3
```

Results:

- `make test`: `asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for ../../../game-files/base`.
- `make sdl3-info test-sdl3`: `HAVE_SDL3=no`; SDL3 smoke skipped gracefully because SDL3 development files are unavailable in this worktree.

## Blockers

None for the headless audio offset validation work. SDL3-backed smoke remains gated on SDL3 dev files being installed or bootstrapped.
