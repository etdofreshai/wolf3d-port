# Parallel Wave Note: 20260425-115713 / zai-glm-5.1

## Cross-Model Review

Reviewed `99c9bb634f77db25e903977d49161906acff1393..HEAD`, including anthropic/claude-opus-4.7 commit `b26a803` and merge `1e88605`.

- The present-frame RGBA helper correctly reuses descriptor-owned indexed pixels/palette, validates dimensions/pitch/palette bounds, and keeps SDL3 smoke code on the consolidated presentation path.
- No source/original or proprietary game-data changes were present.
- No corrective issue found; continued with the next small audio/playback characterization seam.

## Work Done

- Added `wl_imf_music_command` and `wl_get_imf_music_command()` to decode bounded IMF `(register, value, delay)` commands by index.
- Reused the accessor inside `wl_describe_imf_music_chunk()` so summary metadata and future playback scheduling share the same command decoding path.
- Added WL6 assertions for first/last/out-of-range command access on representative music chunk 261.
- Updated the audio research note with the command accessor seam.

## Verification

```bash
cd source/modern-c-sdl3 && make test
cd source/modern-c-sdl3 && make sdl3-info test-sdl3
```

Result:

```text
asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for ../../../game-files/base
HAVE_SDL3=no
SDL3 not available; skipping SDL3 smoke test.
```

## Blockers

- SDL3 development files are not available in this worktree, so SDL3 smoke tests only verify the graceful skip path.
