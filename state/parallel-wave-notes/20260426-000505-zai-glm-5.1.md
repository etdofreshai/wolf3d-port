# Parallel Wave Note — 20260426-000505 — zai/glm-5.1

## Cross-model review

- Reviewed requested range `893295b9294cb3aca5bc0ff672161d7957a84bf9..HEAD`; it contained zero commits in this worktree, so no prior-model code review was needed for this cycle.
- Read `docs/AUTOPILOT.md`, `state/autopilot.md`, and latest parallel notes. Current state points at deeper runtime actor state, boss info-tile coverage, SDL3 presentation, or presentation/helper consolidation.

## Work performed

- Added explicit WL6 AUDIOHED corruption coverage for a non-monotonic mid-table offset.
- The test now confirms both `wl_validate_audio_header_offsets()` and the guarded `wl_read_audio_chunk()` path reject a chunk whose end offset moves backwards before any AUDIOT read.

## Verification

```bash
cd source/modern-c-sdl3
make test
```

Result: asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed.

## Blockers

None.
