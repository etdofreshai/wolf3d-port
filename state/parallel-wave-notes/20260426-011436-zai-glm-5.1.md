# Parallel Wave Note: 20260426-011436 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `70ddd4ab1844ddccf7a5ef67521d22cfc93b98d7..HEAD`; this isolated worktree had zero commits in that range, so no prior-model corrective patch was required.

Also inspected `docs/AUTOPILOT.md`, `state/autopilot.md`, and recent parallel-wave notes. Recorded other-model heads were present in the prompt, but no actual commits from those models were reachable in this worktree range. No concrete correctness, test, docs drift, unsafe asset, or `source/original/` issue was found.

## Work Done

- Added `wl_summarize_actor_wake_state()` as a compact non-mutating AI wake eligibility diagnostic.
- The summary partitions runtime actors into immediately wakeable, ambush-waiting, already chasing, and ineligible buckets, with an `include_ambush` switch matching the mutating wake pass.
- Extended broader WL6 map/boss/secret coverage to cross-check ambush-excluded and ambush-included summaries against actor descriptors.
- Updated the modern C README and runtime model research notes.

## Verification

```bash
cd source/modern-c-sdl3
make test
make sdl3-info test-sdl3
```

Result: headless asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for `../../../game-files/base`; SDL3 smoke skipped gracefully with `HAVE_SDL3=no` because SDL3 development files are unavailable in this worktree.

## Blockers

None for this wake-summary seam. SDL3-backed smoke remains gated on SDL3 development files.
