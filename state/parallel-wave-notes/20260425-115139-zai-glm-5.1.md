# Parallel Wave Note: 20260425-115139 / zai-glm-5.1

## Cross-Model Review

Reviewed `41df0068dbcbdc1fd791a59fd64a2eeca8f5f7f9..HEAD`, which contained Anthropic's `d0cd230` data-directory discovery commit plus merge `f284afd`. The source change is small and correct for isolated parallel worktrees: `WOLF3D_DATA_DIR` still wins, then relative candidates are probed without copying or committing ignored game data. No corrective issue found.

## Work Done

- Extended IMF music chunk metadata with last-command register/value/delay, maximum delay, and zero-delay command counts.
- Added WL6 assertions for representative first/last music chunks so the AdLib/IMF timing stream has a stronger headless characterization seam before SDL3 audio playback work.
- Updated the audio research note with the new asserted timing metadata.

## Verification

```bash
cd source/modern-c-sdl3 && make test
cd source/modern-c-sdl3 && make sdl3-info test-sdl3
```

Result:

```text
asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for ../../../game-files/base
HAVE_SDL3=no
SDL3 not available; skipping SDL3 smoke test.
```

## Blockers

- SDL3 development files are not available in this worktree, so SDL3 smoke tests only verify the graceful skip path.
