# Parallel Wave Note: 20260425-204253 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `f01425556d8aa63046da7feddafd351adf79efb3..HEAD`. The only non-merge prior model commit was `ddc7b43` (`anthropic/claude-opus-4.7`), bridging live player fire ticks into attack startup. The code adds attack metadata to the live fire result, starts weapon attack frames only on successful fire, leaves no-ammo/no-fire cases unchanged, and extends headless assertions. Docs/state are aligned; no unsafe asset/source-original changes were found. No corrective patch was needed.

## Work Done

- Added `wl_present_frame_rgba_size()` so SDL/presentation callers can preflight the exact RGBA upload buffer size from a present-frame descriptor before allocating or uploading.
- Reused the same descriptor validation in `wl_expand_present_frame_to_rgba()` and added undersized-buffer/invalid-descriptor coverage.
- Updated presentation research notes with the RGBA-size preflight seam.

## Verification

```bash
cd source/modern-c-sdl3
make test
```

## Blockers

None. SDL3 development files may still be unavailable on some headless workers; this change is SDL-free.
