# Parallel Wave Note: 20260425-190147 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `dca8634d57ffe961e8bf02e25327a00cf713904d..HEAD`, including `anthropic/claude-opus-4.7` commit `56ccb82` and merge `1faf8d2`.

- The added PC speaker/AdLib sample playback-window helpers reuse the bounded sample accessors, validate chunk lengths and start positions, and have WL6 assertions for mid-window, completion, and invalid-start cases.
- Docs match the implemented audio seam, and no `source/original/` or proprietary game-file assets were changed.
- No corrective issue was found, so this cycle continued with the adjacent AdLib frame-to-frame cursor seam.

## Work Done

- Added `wl_adlib_playback_cursor` and `wl_advance_adlib_playback_cursor()` for deterministic frame-to-frame AdLib sample progression.
- The helper advances a sample index by a sample/frame delta, clamps at completion, reports consumed samples and current sample when active, and rejects invalid completed-cursor deltas or truncated chunks.
- Added WL6 assertions for zero-delta current sample, last-sample progression, clamped completion, completed no-op, invalid completed delta, and truncated-chunk rejection on AdLib sound chunk 87.
- Updated `docs/research/audio-assets.md` with the AdLib playback cursor seam.

## Verification

```bash
cd source/modern-c-sdl3
make test && make sdl3-info test-sdl3
```

Result:

```text
asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for ../../../game-files/base
HAVE_SDL3=no
SDL3 not available; skipping SDL3 smoke test.
```

## Blockers

- SDL3 development files are expected to be unavailable in this isolated worktree, so SDL3 smoke coverage may remain limited to the graceful skip path.
