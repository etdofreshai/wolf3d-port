# Parallel Wave Note: 20260426-015530 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `e6f4b399c5a38ca1215b5f26291fe11e60b8ba45..HEAD`; this isolated worktree had zero commits in that range, so no prior-model corrective patch was required.

Also inspected `docs/AUTOPILOT.md`, `state/autopilot.md`, latest parallel-wave notes, and the recorded other-model heads (`anthropic/claude-opus-4.7@c5644ce`, `openai-codex/gpt-5.5@1ebc836`). Current state and notes remain consistent with headless-first verification, no `source/original/` edits, and no game-file assets committed. No concrete review issue was found before continuing.

## Work Done

- Aligned `wl_classify_map_semantics()` with the SOD runtime-model special actor classifications by recognizing Spear boss/special tiles `107`, `125`, `142`, `143`, and `161` as boss starts, and Spectre tile `106` as ghost-style starts.
- Added optional SOD representative-map semantics assertions before runtime model construction, checking zero unknown info tiles and pinning that direct Spear boss/ghost info-tile counts are covered by the classifier.
- Updated SOD runtime model gap notes with the semantics alignment.

## Verification

```bash
cd source/modern-c-sdl3
make test
make sdl3-info test-sdl3
```

Result: headless asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for `../../../game-files/base`; SDL3 smoke skipped gracefully with `HAVE_SDL3=no` because SDL3 development files are unavailable in this worktree.

## Blockers

None for this SOD semantics alignment. SDL3-backed smoke remains gated on SDL3 development files.
