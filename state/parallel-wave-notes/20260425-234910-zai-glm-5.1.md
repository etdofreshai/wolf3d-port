# Parallel Wave Note: 20260425-234910 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `b412e86d8c9ea8e8ce8ef81b2a306c2b183e7378..HEAD`; this worktree started at `b412e86`, so there were zero commits in range. No corrective range-review patch was needed.

Also inspected `state/autopilot.md`, latest parallel notes, and the named prior merge commits (`anthropic/claude-opus-4.7@c5644ce`, `openai-codex/gpt-5.5@5f9cfae`) for context. Prior work was test-backed, did not touch `source/original/`, and did not commit proprietary assets. No concrete issue was found.

## Work Done

- Added `wl_expand_present_frame_to_rgba_pitched()` so future SDL3 surface/texture paths can expand present-frame indexed pixels into RGBA buffers with caller-provided row pitch.
- Kept `wl_expand_present_frame_to_rgba()` as the tight-row wrapper through the new pitched helper.
- Added headless assertions that padded visible pixels match the tight upload, padding bytes remain untouched, and undersized pitch/buffer inputs fail.
- Documented the pitched RGBA presentation seam in `docs/research/sdl3-bootstrap.md`.

## Verification

```bash
cd source/modern-c-sdl3
make test && make sdl3-info test-sdl3
```

Result: headless asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for `../../../game-files/base`; SDL3 smoke gracefully skipped with `HAVE_SDL3=no` because SDL3 development files are unavailable in this worktree.

## Blockers

None. SDL3-backed presentation remains limited to skip behavior on workers without SDL3 development files.
