# Parallel Wave Note: 20260425-235635 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `fd2f918fe6c53471289d81ff5c9aea7a587667e9..HEAD`; this isolated worktree had zero commits in that range, so no range-review code patch was required before continuing.

Also inspected `state/autopilot.md`, latest parallel notes, and the recorded other-model heads (`anthropic/claude-opus-4.7@c5644ce`, `openai-codex/gpt-5.5@5f9cfae`). Recent presentation RGBA layout/upload/pitched work is SDL-free, test-backed, and avoids `source/original/` and proprietary game-file assets.

Review finding / follow-up: while extending the current pitched-upload seam, I found the descriptor path could accept a buffer that only covered the last visible row (`pitch * (height - 1) + tight_pitch`) while reporting `pixel_bytes = pitch * height`. That could overstate the accessible upload span for future SDL3 callers.

## Work Done

- Tightened `wl_describe_present_frame_rgba_upload_pitched()` to require the caller buffer to cover the full `rgba_pitch * height` span it reports in the upload descriptor.
- Kept `wl_expand_present_frame_to_rgba_pitched(..., out = NULL)` able to use the smaller last-visible-row minimum for callers that only need expansion and intentionally omit trailing padding bytes.
- Added headless assertions pinning the distinction: minimal last-row buffers may expand without a descriptor, but descriptor-producing paths reject them.
- Documented the full-span descriptor contract in `docs/research/sdl3-bootstrap.md`.

## Verification

```bash
cd source/modern-c-sdl3
make test && make sdl3-info test-sdl3
```

Result: headless asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for `../../../game-files/base`; SDL3 smoke skipped gracefully with `HAVE_SDL3=no` because SDL3 development files are unavailable in this worktree.

## Blockers

None for this headless presentation-contract fix. SDL3-backed smoke remains gated on SDL3 development files.
