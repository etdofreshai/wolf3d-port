# Parallel Wave Note: 20260425-203504 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `e19c9e8bb3732f5aea848ad5fc0f6f33cf2d1b1e..HEAD`; the concrete latest prior-model work was `anthropic/claude-opus-4.7@8b6db0b`, especially worker commit `7911c91` (`Add player attack tick seam`).

Review finding: prior work looks sound. The attack-frame tick helper is SDL-free, clamps completion, restores chosen weapons only after a temporary knife fallback with ammo available, is routed through live tick metadata, and has headless coverage. No `source/original/` edits or proprietary game-file assets were present. No corrective fix was needed.

## Work Done

- Added `wl_try_player_fire_weapon_attack()` as a small deterministic bridge between the player fire/ammo decision and attack-frame state.
- The helper wraps `wl_try_player_fire_weapon()`, starts `attack_frame` only when a shot/knife attack actually fires, leaves no-ammo fallback frames unchanged, and reports frame before/after metadata.
- Added headless assertions for successful machinegun fire starting an attack frame, empty-pistol fallback preserving the existing frame, and invalid attack-duration rejection.
- Updated `docs/research/runtime-map-model.md` with the new player fire/attack-frame seam.

## Verification

```bash
cd source/modern-c-sdl3 && make test
cd source/modern-c-sdl3 && make sdl3-info test-sdl3
```

Result: headless asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for `../../../game-files/base`; SDL3 smoke gracefully skipped with `HAVE_SDL3=no` because SDL3 development files are unavailable in this worktree.

## Blockers

None for this SDL-free gameplay seam. SDL3-backed smoke remains limited to graceful skip behavior until SDL3 development files are available.
