# Parallel Wave Note — 20260426-013814 — zai/glm-5.1

## Cross-model review

- Reviewed requested range `d07daff305586d4f9fdced1437bb5e38a08fba31..HEAD`; this isolated worktree started at `d07daff`, so there were zero commits in range and no prior-model corrective patch was required.
- Read `docs/AUTOPILOT.md`, `state/autopilot.md`, and current `state/parallel-wave-notes/`. Latest recorded actor/presentation/audio/SOD work is headless-test-backed, avoids `source/original/`, and does not commit game-file assets.

## Work completed

- Extended `wl_actor_engagement_summary` with farthest active threat index and Manhattan tile distance, complementing the existing nearest-threat fields.
- Updated `wl_summarize_actor_engagements()` to maintain nearest/farthest threat distance range and reset both sentinels for zero-threat summaries.
- Added headless synthetic assertions for farthest-threat selection and no-threat sentinel reset.
- Updated runtime model notes and the modern C README.

## Verification

```bash
cd source/modern-c-sdl3
make test && make sdl3-info test-sdl3
```

Result: headless asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for `../../../game-files/base`; SDL3 smoke skipped gracefully with `HAVE_SDL3=no` because SDL3 development files are unavailable in this worktree.

## Blockers

None. SDL3-backed smoke remains gated on SDL3 development files when unavailable.
