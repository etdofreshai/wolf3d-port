# Parallel Wave Note: 20260425-123244 / zai-glm-5.1

## Cross-Model Review

Reviewed `7ccf1b6abb00793ed8d1a7aa3ddb465131aba091..HEAD`, including anthropic/claude-opus-4.7 commit `1f3af9d` and merge `489077f`.

- The AdLib sound descriptor seam is bounded, validates the common sound header plus instrument block, and is covered by headless WL6 assertions.
- The audio research note matches the implemented metadata seam.
- No source/original or proprietary game-file assets were touched.
- No corrective issue found; continued with a small AdLib playback-readiness seam.

## Work Done

- Added `wl_get_adlib_sound_sample()` to return a bounded AdLib sound sample byte by index after validating the declared sample count and chunk bounds.
- Reused the accessor in `wl_describe_adlib_sound()` for first/last sample extraction so descriptor and future playback code share the same offset logic.
- Added WL6 assertions for first/last/out-of-range sample access on representative AdLib chunk 87, plus a truncated-chunk rejection path.
- Updated `docs/research/audio-assets.md` with the bounded AdLib sample accessor seam.

## Verification

```bash
cd source/modern-c-sdl3 && make test
cd source/modern-c-sdl3 && make sdl3-info test-sdl3
```

## Blockers

- SDL3 development files are not available in this worktree, so SDL3 smoke coverage remains limited to the graceful skip path.
