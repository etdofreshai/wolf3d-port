# Parallel Wave Note: 20260425-122515 / zai-glm-5.1

## Cross-Model Review

Reviewed `ab256face11d7da4edec4703287cd30d6e753987..HEAD`, including anthropic/claude-opus-4.7 recorded commit `1e88605` and current commit `f653977`.

- The latest supervisor script change is outside the port runtime, compiles, keeps the wave counter state ignored, and documents the new numbered-wave/model-stats reporting behavior.
- It updates `state/autopilot.md`, but that was prior mainline/supervisor state work rather than this worker's parallel-wave progress; this worker kept new notes in `state/parallel-wave-notes/` as requested.
- No source/original or proprietary game-file assets were touched, and no corrective issue was found.

## Work Done

- Added `wl_pc_speaker_sound_metadata` and `wl_describe_pc_speaker_sound()` as a bounded PC speaker sound descriptor seam.
- The helper validates the declared sample count against the chunk payload, reports first/last sample bytes, captures the terminating sentinel, and records trailing bytes.
- Added WL6 assertions for representative PC speaker chunks 0 and 1, including a truncated-chunk rejection path.
- Updated `docs/research/audio-assets.md` with the verified PC speaker sample metadata.

## Verification

```bash
cd source/modern-c-sdl3 && make test
cd source/modern-c-sdl3 && make sdl3-info test-sdl3
python3 -m py_compile scripts/wolf3d_parallel_wave.py
```

Result:

```text
asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for ../../../game-files/base
HAVE_SDL3=no
SDL3 not available; skipping SDL3 smoke test.
```

## Blockers

- SDL3 development files are unavailable in this worktree, so SDL3 smoke tests only verify graceful skip behavior.
