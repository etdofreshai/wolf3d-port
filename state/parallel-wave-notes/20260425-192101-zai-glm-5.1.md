# Parallel Wave Note: 20260425-192101 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `e00a560adec090ebe49936fb3c1ee21e757daa06..HEAD`, including `anthropic/claude-opus-4.7` commit `ef24284` and merge `95f2b7f`.

- The prior SOD unknown-info diagnostics are bounded to runtime model metadata, preserve existing unknown counts, and add stable hash/first-unknown assertions for optional SOD maps.
- Docs and tests match the implemented diagnostic seam; no `source/original/` or proprietary game-file assets were changed.
- No corrective issue found. Continued with a small adjacent diagnostic improvement.

## Work Done

- Extended `wl_game_model` unknown-info diagnostics with the last unknown tile/x/y descriptor in addition to the existing count, hash, and first descriptor.
- Pinned last-unknown descriptors for representative optional SOD maps (`Tunnels 1`, `Tunnel Boss`, `Death Knight`, `Angel of Death`) in headless assertions.
- Updated `docs/research/sod-runtime-model-gaps.md` to describe first+last unknown descriptors.

## Verification

```bash
cd source/modern-c-sdl3
make test && make sdl3-info test-sdl3
```

Result: headless asset/decompression/runtime/audio/SOD tests passed for `../../../game-files/base`; SDL3 smoke gracefully skipped with `HAVE_SDL3=no` because SDL3 development files are unavailable in this worktree.

## Blockers

- None for this headless SOD diagnostics seam. SDL3-backed smoke remains limited to graceful skip behavior until SDL3 development files are available.
