# Parallel Wave Note: 20260425-185741 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `e9a47c9ac95c474aac22a19f191c1783d78210af..HEAD`. The concrete prior model work was `anthropic/claude-opus-4.7` commit `4a6d90b` / merge `30f4e4a`:

- Added `wl_get_adlib_instrument_byte()` and reused it from `wl_describe_adlib_sound()`.
- Added WL6 assertions for first/last/out-of-range instrument bytes and truncated chunks.
- Updated `docs/research/audio-assets.md`.

Review finding: prior work is sound. Bounds checks reject short chunks and out-of-range instrument indexes, tests cover the new accessor, and no proprietary assets or `source/original/` changes were made. No corrective fix was needed.

## Work Done

- Added `wl_pc_speaker_playback_cursor` and `wl_advance_pc_speaker_playback_cursor()` for deterministic frame-to-frame PC speaker sample progression.
- The helper advances a sample index by a sample/frame delta, clamps at sound completion, reports consumed samples, exposes the current sample when still active, and rejects invalid completed-cursor deltas/truncated chunks.
- Added WL6 assertions for zero-delta current sample, last-sample progression, clamped completion, completed no-op, invalid completed delta, and truncated-chunk rejection.
- Updated `docs/research/audio-assets.md` with the PC speaker playback cursor seam.

## Verification

```bash
cd source/modern-c-sdl3
make test && make sdl3-info test-sdl3
```

Result: headless asset/decompression/runtime/audio tests passed for `../../../game-files/base`; SDL3 smoke gracefully skipped with `HAVE_SDL3=no` because SDL3 development files are unavailable in this worktree.

## Blockers

- None for this headless seam. SDL3 smoke remains limited to the graceful skip path until SDL3 development files are installed or bootstrapped.
