# Parallel Wave Note: 20260425-114722 / zai-glm-5.1

## Cross-Model Review

Reviewed `0a9473c6f750172016536ea55299f771900f6d94..HEAD`, including anthropic/claude-opus-4.7 commit `29ec5fc` and merge `5b656a6`.

- The SOD runtime gap note is docs-only, contains no proprietary asset bytes, and its warning against broad WL6 static-range widening is consistent with the current path-marker/pushwall tile handling.
- No corrective issue was found, so this cycle added test coverage that pins the documented optional SOD model-gap counts.

## Work Done

- Added optional SOD runtime-model gap assertions to `check_optional_sod()`.
- The assertions decode representative SOD maps (`Tunnels 1`, `Tunnel Boss`, `Death Knight`, `Angel of Death`) through the current WL6-first `wl_build_game_model()` path and pin player position plus door/static/actor/treasure/secret/unknown-info counts.
- Corrected the scan's `Death Knight` map index in executable coverage (`17`; map index `18` is `Secret 1`).

## Verification

```bash
cd source/modern-c-sdl3
WOLF3D_DATA_DIR='/home/node/.openclaw/tmp/Wolfenstein 3D port/game-files/base' make test
```

Result:

```text
asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for /home/node/.openclaw/tmp/Wolfenstein 3D port/game-files/base
```

## Blockers

- SDL3 remains unavailable in this worktree, so SDL3 smoke tests are still limited to the graceful skip path unless bootstrapped.
- SOD unknown-info counts remain intentionally pinned as gaps until source-backed SOD tile mappings are added.
