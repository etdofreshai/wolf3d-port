# Parallel Wave Note: 20260425-204851 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `9f791da6bcff28e2f62774c4e71a2695418cfb28..HEAD`. The only prior-model commit in range was `ba28eed` (`anthropic/claude-opus-4.7`), which blocks no-ammo refire attempts during active attack frames. The patch is localized to `wl_step_live_player_fire_tick()`, preserves in-progress weapon/attack state, updates regression assertions, and does not touch `source/original/` or proprietary assets. No corrective patch was required.

## Work Done

- Source-confirmed SOD boss/special info tiles from original `WL_GAME.C::ScanInfoPlane` mappings: `106` Spectre, `107` Angel, `125` Trans, `142` Uber, `143` Will, and `161` Death.
- Classified those SOD tiles as runtime boss actors in `wl_build_game_model()`.
- Updated optional SOD model assertions and the all-map unknown histogram: remaining unknowns are now only static-like tiles `72..74` (`383` placements / `3` unique IDs).
- Updated `docs/research/sod-runtime-model-gaps.md` with the reduced gap set and next recommendation.

## Verification

```bash
cd source/modern-c-sdl3
make test
```

Result: `asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for ../../../game-files/base`.

## Blockers

None. Remaining SOD model gap is source-confirming static traits for tiles `72..74`.
