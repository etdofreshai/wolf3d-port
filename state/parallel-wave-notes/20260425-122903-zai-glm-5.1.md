# Parallel Wave Note: 20260425-122903 / zai-glm-5.1

## Cross-Model Review

Reviewed `55ea3afb1ab57f4d8f052552df422c534b03fc47..HEAD`, including anthropic/claude-opus-4.7 commit `cefe612`.

- The IMF playback-window descriptor validates declared byte counts, command alignment, chunk bounds, start-index bounds, and end-of-stream behavior.
- Tests cover first-command, mid-stream, multi-command, end-of-stream, and invalid-start windows; docs match the implemented audio seam.
- No source/original or proprietary game-data changes were present.
- No corrective issue found; continued with the next small audio scheduling characterization seam.

## Work Done

- Added `wl_imf_playback_position` and `wl_describe_imf_playback_position()` to map an absolute IMF tick position to the active command, elapsed delay, remaining delay, and completion state.
- Added WL6 assertions for command-boundary positions on music chunk 261: tick 0, one tick before the first command completes, first boundary, second-command remainder, third boundary, and total-delay completion.
- Updated `docs/research/audio-assets.md` with the playback-position seam and verified metadata.

## Verification

```bash
cd source/modern-c-sdl3 && make test
cd source/modern-c-sdl3 && make test-sdl3
```

Results:

```text
asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for ../../../game-files/base
SDL3 not available; skipping SDL3 smoke test.
Run ../../scripts/bootstrap_sdl3.sh or set PKG_CONFIG_PATH to a directory containing sdl3.pc.
```

## Blockers

- SDL3 development files are not available in this worktree, so SDL3 smoke coverage remains limited to the existing graceful skip path.
