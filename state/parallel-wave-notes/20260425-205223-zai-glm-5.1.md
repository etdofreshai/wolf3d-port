# Parallel Wave Note: 20260425-205223 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `9b140c6422cd9da10571ce20dd0d567d2ca07a86..HEAD` before new work.

- Concrete prior model work in range: `anthropic/claude-opus-4.7@8fc8c41` (`Expose active attack fire block metadata`), merged at `bea7d58`.
- The new `fire_blocked_by_active_attack` metadata and docs mostly matched the implementation, and the headless test suite passed before changes.
- Review finding: the implementation checked `state->attack_frame` only after `wl_step_live_tick()`, so a fire press could start a fresh shot on the same tick where a previously active attack frame reached zero. That undercut the documented/desired active-frame refire suppression.

## Work Done

- Fixed `wl_step_live_player_fire_tick()` to remember the attack frame at tick entry and suppress same-tick refire whenever an attack was active before the live tick advanced.
- Preserved post-tick frame metadata in the blocked fire result while reporting the pre-tick active frame as the block source.
- Added headless coverage for the finish-tick suppression edge case and adjusted existing block assertions to pin pre/post frame metadata.
- Updated the modern SDL3 README and runtime map-model research note to document finish-tick refire suppression coverage.

## Verification

```bash
cd source/modern-c-sdl3
make test && make sdl3-info test-sdl3
```

Result: headless asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for `../../../game-files/base`; SDL3 smoke gracefully skipped with `HAVE_SDL3=no` because SDL3 development files are unavailable in this worktree.

## Blockers

None. SDL3-backed smoke remains limited to graceful skip behavior until SDL3 development files are available.
