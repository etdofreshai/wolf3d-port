# Parallel Wave Note — 20260425-193920 — zai/glm-5.1

## Cross-model review

Reviewed `0a5476999ae6a6f165406feb558347eaf32322bd..HEAD`.

- `4b35455` / merged `02357c4` from `anthropic/claude-opus-4.7` added `wl_schedule_sound_channel()` and resolved the audio-doc/test merge with the prior chunk-start seam.
- Verification before new work: `cd source/modern-c-sdl3 && make test` passed.
- Finding: prior work looks sound; no unsafe assets, no `source/original/` edits, and the tests cover accepted/rejected mutable scheduling paths. Small follow-up chosen: mirror the existing chunk-id truncation guard on the scheduling API.

## Work completed

- Added `wl_schedule_sound_channel_from_chunk()`, a bounded size_t chunk-id wrapper over `wl_schedule_sound_channel()` that rejects ids above `UINT16_MAX` before storing in channel state.
- Added headless WL6 coverage for metadata-backed AdLib scheduling from chunk 87 and oversized chunk-id rejection.
- Updated `docs/research/audio-assets.md` with the new scheduling seam and coverage note.

## Verification

- `cd source/modern-c-sdl3 && make test`
- `cd source/modern-c-sdl3 && make sdl3-info test-sdl3` (SDL3 unavailable; target skipped as designed)

## Blockers

None.
