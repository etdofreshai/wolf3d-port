# Parallel Wave Note: 20260426-064842 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `fad51b4a5f6fe9ab0c629f245abdb8e629b3373f..HEAD` at cycle start. This isolated worktree was exactly at `fad51b4`, so the range contained zero commits and no range-specific prior-model corrective review was required.

Also inspected `docs/AUTOPILOT.md`, `state/autopilot.md`, and recent parallel-wave notes. The latest visible path-marker diagnostics are headless-test-backed, avoid `source/original/`, and do not commit proprietary game assets. No concrete corrective issue was found before continuing.

## Work Done

- Extended `wl_path_marker_endpoint_summary` with `isolated_marker_count` and `first_isolated_marker_index`.
- Updated `wl_summarize_path_marker_endpoints()` to identify valid path markers that have no incoming links and no valid outgoing marker link while preserving existing source/sink/branch counters.
- Added synthetic headless assertions for the isolated endpoint bucket and first-index sentinel behavior.
- Updated runtime model research notes and the modern C README.

## Verification

```bash
cd source/modern-c-sdl3
make test
```

Result: asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for `../../../game-files/base`; optional SDL3 smoke skipped gracefully because SDL3 development files are unavailable in this worktree.

## Blockers

None currently. SDL3-backed smoke remains gated on SDL3 development files when unavailable.
