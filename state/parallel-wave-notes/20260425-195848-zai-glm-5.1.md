# Parallel Wave Note: 20260425-195848 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `6189d0e2a6fee20e96b9034c5d5b5f1c78c6a0ce..HEAD` before new work. Concrete prior model work in range was `anthropic/claude-opus-4.7@7726605` (`Add metadata-backed AdLib register descriptor`), merged at `b51f099`.

- The prior helper validates music/digital/empty/truncated rejection before dispatching AdLib instrument bytes into the existing OPL register descriptor seam.
- Tests pin valid WL6 AdLib register values and invalid metadata cases; docs match the implementation.
- No `source/original/` edits or proprietary game-file assets were present. No corrective issue was found.

## Work Done

- Added metadata-backed IMF music playback wrappers:
  - `wl_describe_imf_playback_position_from_chunk()`
  - `wl_advance_imf_playback_cursor_from_chunk()`
- The wrappers validate music chunk metadata and reject non-music, empty, impossible payload, and malformed metadata before dispatching to existing deterministic IMF seek/cursor helpers.
- Added WL6 assertions for valid metadata-backed tick-position seek and cursor advancement, plus invalid non-music and impossible-payload rejection.
- Updated `docs/research/audio-assets.md` with the new metadata-backed music scheduler seam.

## Verification

```bash
cd source/modern-c-sdl3
make test && make sdl3-info test-sdl3
```

Result: headless asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for `../../../game-files/base`; SDL3 smoke gracefully skipped with `HAVE_SDL3=no` because SDL3 development files are unavailable in this worktree.

## Blockers

None for this SDL-free music playback metadata seam. SDL3-backed smoke remains limited to graceful skip behavior until SDL3 development files are available.
