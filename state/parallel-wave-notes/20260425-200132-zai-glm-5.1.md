# Parallel Wave Note: 20260425-200132 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `a5a3eae5d2f2c224b613ff5a9ffe3587860245d5..HEAD` before new work. Concrete prior model work in range was `anthropic/claude-opus-4.7@eed9db9`, especially worker commit `b30b83b` (`Add metadata-backed sound channel window seam`).

- The prior helper validates active channel state and reuses the metadata-backed PC speaker/AdLib playback-window dispatcher from the current cursor without mutating state.
- Tests cover an active PC-speaker terminal window and inactive-channel rejection; docs match the new seam.
- No `source/original/` edits or proprietary game-file assets were present. No corrective issue was found.

## Work Done

- Added `wl_describe_sound_channel_remaining_from_chunk()` as a tiny metadata-backed active-channel inspection seam for future mixer buffer sizing.
- The helper validates an active channel cursor against PC speaker/AdLib sample counts, reports remaining samples, and rejects inactive/invalid active state or out-of-range cursors.
- Added WL6 headless assertions for PC speaker and AdLib remaining-sample inspection plus inactive/invalid/out-of-range rejection.
- Updated `docs/research/audio-assets.md` with the remaining-sample seam.

## Verification

```bash
cd source/modern-c-sdl3
make test && make sdl3-info test-sdl3
```

Result: headless asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for `../../../game-files/base`; SDL3 smoke gracefully skipped with `HAVE_SDL3=no` because SDL3 development files are unavailable in this worktree.

## Blockers

None for this SDL-free audio inspection seam. SDL3-backed smoke remains limited to graceful skip behavior until SDL3 development files are available.
