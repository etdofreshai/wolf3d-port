# Parallel Wave Note: 20260426-005654 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `e68a88545775bb28299b3a8a476bd16308dd3efd..HEAD`; this isolated worktree had zero commits in that range, so no prior-model corrective patch was required.

Also inspected `docs/AUTOPILOT.md`, `state/autopilot.md`, latest parallel-wave notes, and the recorded other-model heads (`anthropic/claude-opus-4.7@c5644ce`, `openai-codex/gpt-5.5@cd5933a`). Recent SOD runtime, presentation, actor wake, and audio metadata work is headless-test-backed and does not touch `source/original/` or proprietary game-file assets. No concrete review issue was found before continuing.

## Work Done

- Hardened metadata-backed IMF music position/cursor wrappers with a shared payload-bounds validator.
- The wrappers now reject descriptors whose `payload_offset + payload_size` would exceed the actual chunk buffer, including the edge case where `payload_size == chunk_size` but the IMF payload starts after the 4-byte length header.
- Added WL6 regression coverage for position, looped-position, and cursor wrappers on that impossible payload descriptor.
- Updated audio research notes with the stricter metadata-backed IMF payload contract.

## Verification

```bash
cd source/modern-c-sdl3
make test && make sdl3-info test-sdl3
```

Result: headless asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for `../../../game-files/base`; SDL3 smoke skipped gracefully with `HAVE_SDL3=no` because SDL3 development files are unavailable in this worktree.

## Blockers

None for this headless audio metadata hardening. SDL3-backed smoke remains gated on SDL3 development files.
