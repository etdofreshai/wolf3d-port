# Parallel Wave Note: 20260425-112212 / zai-glm-5.1

## Sanity check

- Reviewed `docs/AUTOPILOT.md` and latest `state/autopilot.md`.
- `state/parallel-wave-notes/` was absent at start of this worktree, so this note creates the wave-local progress record.
- Initial git status was clean; latest recorded commit was `c324c69 Avoid autopilot state conflicts in waves`.

## Work completed

- Classified info-plane exit trigger markers (`WL_EXITTILE`, tile 99) as known runtime map specials instead of unknown info tiles.
- Mirrored that classification in map semantics by counting info-plane exit markers as `exit_tiles`.
- Expanded WL6 boss-map runtime coverage to include `Wolf1 Boss` and `Wolf5 Boss`, with zero-unknown assertions, actor-kind counts, scene-ref counts, and deterministic scene-ref hashes.
- Updated runtime map model research notes to reflect the completed Wolf1/Wolf5 boss-map classification pass.

## Verification

- `WOLF3D_DATA_DIR='/home/node/.openclaw/tmp/Wolfenstein 3D port/game-files/base' make test` from `source/modern-c-sdl3` passed.
- `make sdl3-info test-sdl3` reported `HAVE_SDL3=no` and skipped SDL3 smoke tests as expected in this worktree.

## Review findings

- The prior boss coverage already handled boss actor spawn tiles; the remaining Wolf1/Wolf5 unknowns were original exit triggers placed on the info plane, not new actor types.
- Plain `make test` fails in this isolated worktree unless `WOLF3D_DATA_DIR` points at the main local game data directory, because proprietary game files are intentionally not present in the worktree.

## Blockers

- SDL3 is not installed/bootstrapped in this worktree, so SDL3 presentation tests could only be checked for graceful skip behavior.
