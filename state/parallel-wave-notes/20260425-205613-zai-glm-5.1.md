# Parallel Wave Note: 20260425-205613 / zai-glm-5.1

## Cross-Model Review

Reviewed requested range `53117e56935b765c872d08f0c2fd8e911544abd8..HEAD` before new work. The concrete prior model work in this worktree was `anthropic/claude-opus-4.7@6af114f` (`Classify remaining SOD static info tiles`).

Review finding: prior work looks sound. The SOD static classifications are source-backed, keep the unsafe path-marker/pushwall ranges narrow, reduce optional SOD unknown-info coverage to zero, update docs, and do not touch `source/original/` or proprietary game-file assets. No corrective patch was needed.

## Work Done

- Broadened optional SOD runtime scene coverage now that SOD static/boss info-plane gaps are closed.
- Added scene-ref count/hash assertions for representative SOD maps (`Tunnels 1`, `Tunnel Boss`, `Death Knight`, `Angel of Death`) through `wl_collect_scene_sprite_refs()` using the SOD VSWAP sprite start.
- Pinned representative Spear static type placements for newly classified static types, including marble pillars and the truck static.
- Updated `docs/research/sod-runtime-model-gaps.md` with the new renderer-facing SOD scene-ref coverage and next follow-up.

## Verification

```bash
cd source/modern-c-sdl3
make test
make sdl3-info test-sdl3
```

Result: headless asset/decompression/semantics/model/vswap/runtime-present-chase-attack-frame/audio+sod-audio tests passed for `../../../game-files/base`; SDL3 smoke gracefully skipped with `HAVE_SDL3=no` because SDL3 development files are unavailable in this worktree.

## Blockers

None for this headless SOD scene-ref coverage. SDL3-backed smoke remains limited to graceful skip behavior until SDL3 development files are available.
